# Copy this file to apikey.yaml (to be sure that docker sees it as a file and not as a directory)
# The content of apikey.yaml will be replaced by SWAPI on the first run, with the actual API key for your instance
YOUR_SWAPI_ID:
